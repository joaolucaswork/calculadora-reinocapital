# Reino Capital Calculator - Environment Configuration Example
# Copy this file to create environment-specific configurations

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Public Anonymous Key (safe to expose in frontend)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# Service Role Key (KEEP SECRET - server-side only)
# Required for database cleanup and administrative operations
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# =============================================================================
# ENVIRONMENT IDENTIFICATION
# =============================================================================

# Environment type: production, testing, development, staging
ENVIRONMENT=production

# Node.js environment
NODE_ENV=production

# =============================================================================
# TESTING CONFIGURATION (for .env.testing)
# =============================================================================

# Enable debug mode for testing
# DEBUG=true

# Enable test mode flag
# TEST_MODE=true

# Test data patterns
# TEST_EMAIL_SUFFIX=+test
# TEST_PHONE_PREFIX=(11) 99999
# TEST_NAME_PREFIX=Test

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Table name for calculator submissions
TABLE_NAME=calculator_submissions

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Enable/disable certain features based on environment
# ENABLE_ANALYTICS=true
# ENABLE_ERROR_REPORTING=true
# ENABLE_DEBUG_LOGGING=false

# =============================================================================
# INSTRUCTIONS FOR SETUP
# =============================================================================

# 1. Copy this file to create environment-specific configurations:
#    cp .env.example .env.production
#    cp .env.example .env.testing
#    cp .env.example .env.development

# 2. Update the values with your actual Supabase credentials

# 3. For testing environment (.env.testing), add:
#    ENVIRONMENT=testing
#    NODE_ENV=test
#    DEBUG=true
#    TEST_MODE=true

# 4. For development environment (.env.development), add:
#    ENVIRONMENT=development
#    NODE_ENV=development
#    DEBUG=true

# 5. Never commit actual .env files to version control!
#    Add them to .gitignore:
#    .env*
#    !.env.example

# =============================================================================
# SUPABASE SETUP REQUIREMENTS
# =============================================================================

# Your calculator_submissions table must include these columns:
# - environment VARCHAR DEFAULT 'production'
# - created_by VARCHAR DEFAULT 'user'  
# - test_run_id VARCHAR NULL
# - submitted_at TIMESTAMP DEFAULT NOW()

# Row Level Security (RLS) policies should be configured to:
# - Isolate data by environment
# - Restrict access based on user roles
# - Prevent cross-environment data access

# =============================================================================
# SECURITY NOTES
# =============================================================================

# CRITICAL: Never expose service role keys in frontend code
# CRITICAL: Rotate keys regularly (monthly recommended)
# CRITICAL: Use environment-specific keys for each deployment
# CRITICAL: Monitor key usage and access patterns

# For CI/CD environments, store these as encrypted secrets:
# - GitHub Actions: Repository Settings → Secrets and variables
# - GitLab CI: Project Settings → CI/CD → Variables  
# - Azure DevOps: Pipeline → Library → Variable groups
